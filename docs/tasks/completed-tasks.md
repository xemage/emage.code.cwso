# Completed Tasks

Append-only log. Entries move here after the orchestrator marks a task `done`.

| ID | Title | Owner | Done on | Outcome / artifact |
|----|-------|-------|---------|--------------------|
| T001 | PO: User story + acceptance criteria (operator-dashboard) | product-owner | 2026-08-06 | `docs/artifacts/feature-operator-dashboard-requirements-v1.md` |
| T002 | SA: Technical impact assessment + ADR-011 | solution-architect | 2026-08-06 | ADR-011; architecture impact analysis for operator dashboard |
| T003 | SM: Sprint plan + GitLab issues | scrum-master | 2026-08-06 | Sprint plan and GitLab issues created for T004–T010 |
| T004 | BE: Expose Stats() on jobs.Manager | backend-developer | 2026-08-06 | `orchestrator/internal/jobs/manager.go` — `Stats()` method and `JobsSnapshot` type added |
| T005 | BE: Client activity middleware (auth/tool counters) | backend-developer | 2026-08-06 | `orchestrator/internal/transport/http.go` — `RequestMetrics` interface + `countRequestsMiddleware`, auth/rate-limit counters |
| T006 | BE: Config validation + sidecar health aggregator | backend-developer | 2026-08-06 | `orchestrator/internal/config/` — validation logic; sidecar health aggregator wired |
| T007 | BE: Dashboard HTTP routes + embedded HTML | backend-developer | 2026-08-06 | `orchestrator/internal/dashboard/` — `/dashboard` and `/dashboard/status` routes with embedded HTML |
| T008 | TL: Code review gate (CONDITIONAL_PASS → PASS after fixes) | tech-lead | 2026-08-06 | PASS verdict after fixes applied to dashboard package |
| T009 | QA: 13 unit/integration tests for dashboard package | qa-engineer | 2026-08-06 | `orchestrator/internal/dashboard/*_test.go` — 13 tests, all green |
| T180 | BE: Close TD-05/TD-06/TD-08 entries in debt register | backend-developer | 2026-08-08 | `TECHNICAL-DEBT.md` — TD-05/06/08 rows removed from active table; `## Closed Items` section added with T162 provenance |
| T181 | BE: TD-07 sync.Once guard for Broker.Close() | backend-developer | 2026-08-08 | `orchestrator/internal/memorybroker/broker.go` — `closeOnce sync.Once` field added; `Close()` body replaced with `closeOnce.Do`; race-free double-close protection confirmed by `go test -race` |
| T182 | BE: TD-02 introduce HTTPHandlerConfig for RunHTTP/newHTTPHandler | backend-developer | 2026-08-08 | `orchestrator/internal/transport/http.go` — `HTTPHandlerConfig` struct introduced; `RunHTTP`/`newHTTPHandler` signatures reduced to 4 params; all callers updated; `go build ./...` clean |
| T183 | BE: TD-03 reduce helper parameter counts via deps structs | backend-developer | 2026-08-08 | `orchestrator/internal/transport/http.go` — `postHandlerDeps`, `sseHandlerDeps`, `sampleEventParams` structs added; `handlePOST`/`handleSSE`/`publishSampleEvents` reduced to ≤3 params |
| T184 | BE: TD-01 extract SSE helpers and cut function size | backend-developer | 2026-08-08 | `orchestrator/internal/transport/http.go` — `writeBrokerSSEFrame`, `brokerSSETelemetryDefer`, `writeSSEFrame` helpers extracted; `handleBrokerSSE` 44 lines, `handleSSE` 42 lines (both ≤50) |
| T185 | BE: TD-09 evict zero-count sseConns entries | backend-developer | 2026-08-08 | `orchestrator/internal/transport/http.go` — `release()` deletes map entry at zero; `orchestrator/internal/transport/http_test.go` — `TestSSEConnectionStoreEviction` added at line 844 |
| T186 | QA: TD-04 unit test for broker SSE deferred telemetry log | qa-engineer | 2026-08-08 | `orchestrator/internal/transport/http_sse_telemetry_test.go` — `TestBrokerSSETelemetryLogOnClose` with `clean_close_logs_info` (Info level) and `dropped_events_logs_warn` (Warn level) subtests; both assert ready-event; `go test -race` clean |
| T187 | BE: TD-03 residual — reduce handleBrokerSSE to ≤4 params | backend-developer | 2026-08-08 | `orchestrator/internal/transport/http.go` — `brokerSSEDeps` struct added; `handleBrokerSSE` reduced from 5 to 3 params; call site in `handleSSE` updated; 44 lines maintained |
| T188 | QA: TD-10 fix SSE telemetry test stderr-capture race | qa-engineer | 2026-08-08 | `orchestrator/internal/transport/http_sse_telemetry_test.go` — `os.Pipe`/`os.Stderr` redirect removed; `logging.NewWithWriter` used for log capture; drop-path subtest calls `brokerSSETelemetryDefer` directly (no HTTP server); `srv.Close()` used to synchronise before buffer read; passes `-race -count=5` |
| T189 | QA: TD-11 investigate and fix TestRetentionEvictionOldestFirst flakiness | qa-engineer | 2026-08-08 | `orchestrator/internal/memorybroker/broker_test.go` — root cause: `waitForLen(b,3)` returned before all 5 events were processed (async ring-buffer eviction race); fixed by adding `waitForMaxSeq` helper and replacing the wait condition; passes `-race -count=5` |
| T080 | Phase 6 requirements + hardware benchmark targets | product-owner | 2026-06-02 | Migrated from active-tasks.md board cleanup |
| T081 | HAL design: `InferenceBackend` trait + plugin loading + `dispatch.provider/v2` | solution-architect | 2026-06-02 | Migrated from active-tasks.md board cleanup |
| T082 | Rust `cwso-hal` crate + CPU-baseline adapter | backend-developer | 2026-06-02 | Migrated from active-tasks.md board cleanup |
| T083 | GPU adapter (vLLM/TensorRT-LLM, OpenAI-compatible) | backend-developer | 2026-06-02 | Migrated from active-tasks.md board cleanup |
| T084 | LPU adapter (Groq-style deterministic low-latency) | backend-developer | 2026-06-02 | Migrated from active-tasks.md board cleanup |
| T085 | Profiling layer: tensor_tag derivation + workload mapping | backend-developer | 2026-06-02 | Migrated from active-tasks.md board cleanup |
| T086 | `dispatch_hardware_aware_job` MCP tool + schema | backend-developer | 2026-06-02 | Migrated from active-tasks.md board cleanup |
| T087 | Wire policy_engine_v2 to live adapters (remove spike stubs) | backend-developer | 2026-06-02 | Migrated from active-tasks.md board cleanup |
| T088 | Phase 6 integration + reliability QA (fallback ≤ 2.0s, overhead ≤ 10ms) | qa-engineer | 2026-06-02 | Migrated from active-tasks.md board cleanup |
| T089 | Phase 6 Tech-Lead + Security gate | tech-lead / security-engineer | 2026-06-02 | Migrated from active-tasks.md board cleanup |
| T090 | Thread job context into `hal.Client.Infer` (cancellation propagation) | backend-developer | 2026-06-02 | Migrated from active-tasks.md board cleanup |
| T091 | Active HAL health probing → live `health_state`/`queue_depth` | backend-developer | 2026-06-02 | Migrated from active-tasks.md board cleanup |
| T092 | Hardware-aware job result retrieval (poll/stream completion) | backend-developer | 2026-06-02 | Migrated from active-tasks.md board cleanup |
| T093 | Enforce/document TLS for non-loopback HAL accelerator endpoints | devops-engineer | 2026-06-02 | Migrated from active-tasks.md board cleanup |
| T094 | CI dependency audit (`govulncheck` + `cargo audit`) | devops-engineer | 2026-06-02 | Migrated from active-tasks.md board cleanup |
| T114 | Bump Go toolchain to 1.25 (clear `go:audit` stdlib advisories) | devops-engineer | 2026-06-02 | Migrated from active-tasks.md board cleanup |
| T115 | AST write-spike monitor (generalize `anomaly_monitor`) + userspace fallback | backend-developer | 2026-06-03 | Migrated from active-tasks.md board cleanup |
| T116 | Spike filter (semantic classifier) + semantic-conflict pre-warning | backend-developer | 2026-06-03 | Migrated from active-tasks.md board cleanup |
| T117 | `subscribe_ast_spikes` MCP Resources layer (SSE, threshold-gated) | backend-developer | 2026-06-03 | Migrated from active-tasks.md board cleanup |
| T118 | AST write-event feeder wiring (`write_shadow_file` → monitor/filter) | backend-developer | 2026-06-03 | Migrated from active-tasks.md board cleanup |
| T119 | Sparse Wasm micro-agent sandbox tier design + security envelope review | solution-architect | 2026-06-03 | Migrated from active-tasks.md board cleanup |
| T120 | Rust `cwso-sparse` sidecar: deterministic ternary GEMM kernel + UDS protocol | backend-developer | 2026-06-03 | Migrated from active-tasks.md board cleanup |
| T121 | `.cwsl` pruned-slice container + COW mmap loader + SHA-256 pinning | backend-developer | 2026-06-03 | Migrated from active-tasks.md board cleanup |
| T122 | `create_ephemeral_sparse_agent` MCP tool + wasmtime lifecycle + agent telemetry resource | backend-developer | 2026-06-04 | Migrated from active-tasks.md board cleanup |
| T123 | Quality-floor guardrail → dense GPU escalation (reuse `quality_guardrail_autodisable`) | backend-developer | 2026-06-04 | Migrated from active-tasks.md board cleanup |
| T124 | Phase 7 integration QA (cold start < 10 ms, 0% idle CPU, escalation) | qa-engineer | 2026-06-04 | Migrated from active-tasks.md board cleanup |
| T125 | Phase 7 Tech-Lead + Security gate (Feature B + C) | tech-lead / security-engineer | 2026-06-04 | Migrated from active-tasks.md board cleanup |
| T126 | Sparse AST tensor encoding spec (photonic-ready kernel contract) | solution-architect | 2026-06-04 | Migrated from active-tasks.md board cleanup |
| T127 | AVX-512 / `std::simd` sparse diff kernel in cwso-merge-engine | backend-developer | 2026-06-04 | Migrated from active-tasks.md board cleanup |
| T128 | Sparse pre-filter integration (skip shared base subtrees) | backend-developer | 2026-06-04 | Migrated from active-tasks.md board cleanup |
| T129 | Sparse↔dense conflict-matrix conformance suite | qa-engineer | 2026-06-04 | Migrated from active-tasks.md board cleanup |
| T130 | Phase 8 Tech-Lead gate + large-repo merge benchmark | tech-lead | 2026-06-04 | Migrated from active-tasks.md board cleanup |
| T131 | Rollout architecture: proxy boundary + Polar REST API | solution-architect | 2026-06-04 | Migrated from active-tasks.md board cleanup |
| T132 | Rust `hyper` reverse proxy + zero-copy capture | backend-developer | 2026-06-04 | Migrated from active-tasks.md board cleanup |
| T133 | Trajectory builder + prefix merging | backend-developer | 2026-06-05 | Migrated from active-tasks.md board cleanup |
| T134 | Trajectory store (Arrow + LZ4 + Parquet) | backend-developer | 2026-06-05 | Migrated from active-tasks.md board cleanup |
| T136 | Programmatic reward emission (merge SM hook) | backend-developer | 2026-06-05 | Migrated from active-tasks.md board cleanup |
| T137 | Polar REST API + trainer e2e | backend-developer | 2026-06-05 | Migrated from active-tasks.md board cleanup |
| T138 | Phase 9 integration QA + security gate | qa / security | 2026-06-06 | Migrated from active-tasks.md board cleanup |
| T139 | v0.3.0 release readiness (Phases 6–9) | release-manager | 2026-06-06 | Migrated from active-tasks.md board cleanup |
| T135 | KV-cache prefix router | backend-developer | 2026-06-06 | Migrated from active-tasks.md board cleanup |
| T140 | CI audit hardening (promote go:audit/rust:audit to blocking) | devops-engineer | 2026-06-06 | Migrated from active-tasks.md board cleanup |
| T141 | Publish GitLab release v0.3.0-rc1 + GA prep checkpoint | release-manager | 2026-06-07 | Migrated from active-tasks.md board cleanup |
| T143 | Root hygiene & PoC debt archive | tech-lead | 2026-06-07 | Migrated from active-tasks.md board cleanup |
| T142 | Installation & usage documentation | technical-writer | 2026-06-07 | Migrated from active-tasks.md board cleanup |
| T147 | OpenAI Responses API + proxy hardening | backend-developer | 2026-06-07 | Migrated from active-tasks.md board cleanup |
| T152 | v0.3.0 GA release readiness | release-manager | 2026-06-07 | Migrated from active-tasks.md board cleanup |
| T153 | Tag pipeline deploy fix (`needs:optional` e2e) | devops-engineer | 2026-06-07 | Migrated from active-tasks.md board cleanup |
| T144 | Polar harness adapters + runtime launcher | backend-developer | 2026-06-07 | Migrated from active-tasks.md board cleanup |
| T145 | Rollout `num_samples` session fan-out | backend-developer | 2026-06-07 | Migrated from active-tasks.md board cleanup |
| T154 | IDE integration guide (VS Code / Cursor) | technical-writer | 2026-06-07 | Migrated from active-tasks.md board cleanup |
| T155 | Enable-all-features script | devops-engineer | 2026-06-07 | Migrated from active-tasks.md board cleanup |
| T146 | Gateway async staging + partial traces | backend-developer | 2026-06-07 | Migrated from active-tasks.md board cleanup |
| T149 | Trajectory builder Polar parity | backend-developer | 2026-06-09 | Migrated from active-tasks.md board cleanup |
| T148 | Evaluator registry + SWE-bench hook | backend-developer | 2026-06-09 | Migrated from active-tasks.md board cleanup |
| T156 | Comprehensive installation guide v2 (v0.4.0) | technical-writer | 2026-06-09 | Migrated from active-tasks.md board cleanup |
| T157 | v0.4.0 release readiness | release-manager | 2026-06-09 | Migrated from active-tasks.md board cleanup |
| T163 | Hardening and Polar parity validation gate | qa / security / tech-lead | 2026-06-18 | `docs/tasks/task-T163.md`; `docs/artifacts/gate-v0.4.1-hardening-2026-06-18.md` — QA/Security/Tech-Lead verdict `PASS`; `make smoke-local` → `PHASE 2 INTEGRATION TEST: PASS` |
| T150 | KV differential prompting | backend-developer | 2026-06-19 | `docs/tasks/task-T150.md`; `docs/checkpoints/checkpoint-015-v0.4.1-hardening-complete.md`; `services/cwso-rollout/src/capture.rs` — `maybe_apply_kv_differential_prompting()` + test `differential_prompting_strips_prefix_tokens_on_cache_hit` (confirmed present in current tree), gated by `CWSO_ROLLOUT_KV_DIFFERENTIAL_PROMPTING_ENABLED` |
| T151 | Offline SFT data generation mode | backend-developer | 2026-06-19 | `docs/tasks/task-T151.md`; `docs/checkpoints/checkpoint-015-v0.4.1-hardening-complete.md`; `orchestrator/internal/rollout/service.go` (`GenerateOfflineTask`) + `POST /rollout/task/offline_generate` handler; tests `TestGenerateOfflineTaskCompletesWithoutCallback`, `TestHTTPOfflineGenerate` in `orchestrator/internal/rollout/service_test.go` (confirmed present) |
| T158 | Fix local Phase 2 integration auth mismatch | backend-developer | 2026-06-19 | `docs/tasks/task-T158.md`; `docs/checkpoints/checkpoint-015-v0.4.1-hardening-complete.md`; `.env.jwt.dev` canonical local JWT source (confirmed present) + `scripts/phase2-integration.py` `resolve_jwt_secret()`; `make smoke-local` → `PHASE 2 INTEGRATION TEST: PASS` |
| T159 | Add deterministic one-command local smoke target | devops-engineer | 2026-06-19 | `docs/tasks/task-T159.md`; `docs/checkpoints/checkpoint-015-v0.4.1-hardening-complete.md`; `Makefile` `smoke-local` target (confirmed present); `docs/user/installation-v2.md` quick-start references it |
| T160 | Reconcile v0.4.0 GA documentation drift | technical-writer | 2026-06-19 | `docs/tasks/task-T160.md`; `docs/checkpoints/checkpoint-015-v0.4.1-hardening-complete.md`; `docs/checkpoints/checkpoint-014-v0.4.0-ga.md` (updated to reflect published/tagged state); `docs/user/installation-v2.md` |
| T161 | Clean active/completed task board hygiene | scrum-master | 2026-06-19 | `docs/tasks/task-T161.md`; `docs/checkpoints/checkpoint-015-v0.4.1-hardening-complete.md`; original migration landed in commit `ec1c2d4`. NOTE: a later merge/back-merge regressed this — T150-T163 reappeared in `active-tasks.md` as unarchived `done` rows and dropped out of `completed-tasks.md`; this archival (current commit) re-applies the hygiene fix |
| T162 | Remediate high-value reliability/security technical debt | backend-developer | 2026-06-19 | `docs/tasks/task-T162.md`; `docs/checkpoints/checkpoint-015-v0.4.1-hardening-complete.md`; `orchestrator/internal/jobs/manager.go` (TD-05 publish-failure logging, TD-06 `cancelQueuedJobsOnClose`, TD-08 `sanitizeErrorForBroadcast`); tests `TestCloseCancelsQueuedJobs`, `TestPublishLifecycleErrorIsRedacted` in `orchestrator/internal/jobs/manager_test.go` (confirmed present) |
| T164 | Fix flaky TestNodeRegistry_AssignTask | backend-developer | 2026-07-27 | `docs/tasks/task-T164.md`; `orchestrator/internal/rollout/node_registry.go` — `sort.Slice` by `NodeID` added to `getActiveNodesLocked()` (confirmed present in current tree); fix commit `f002ee2` merged via MR `bugfix/T164-flaky-node-registry-assign` into `develop` (merge commit `f7d8841`) |
| T169 | Root-cause investigation: rollout healthcheck 405 and trajectory store path mismatch | backend-developer | 2026-08-01 | `docs/artifacts/root-cause-analysis-cwso-rollout-v1.md` — Issue 1 (405) verdict NEEDS-REFINEMENT (no `/v1/models` route exists under any method; global POST-only gate at `proxy.rs:46-51`; no callers/tests depend on current behavior); Issue 2 (trajectory store path) verdict CONFIRMED (`store.rs:46` reads `CWSO_ROLLOUT_STORE_PATH`, never `CWSO_ROLLOUT_TRAJECTORY_STORE_PATH`, which `deploy/Dockerfile.rollout:27` sets) |
| T170 | Implement and verify fix for confirmed rollout defect(s) | backend-developer | 2026-08-01 | `docs/artifacts/fix-verification-cwso-rollout-v1.md` — added `GET /healthz` liveness route (`services/cwso-rollout/src/proxy.rs`) + `HEALTHCHECK` in `deploy/Dockerfile.rollout`; `StoreConfig::from_env` (`services/cwso-rollout/src/store.rs`) now checks `CWSO_ROLLOUT_TRAJECTORY_STORE_PATH` before `CWSO_ROLLOUT_STORE_PATH`; verified via real `cargo build`/`cargo test` (35/35 pass) and real `docker build`/`docker run`: sustained `(healthy)`, `FailingStreak:0` across 5/5 probes (both explicit health-cmd and Dockerfile-native), trajectory store dir created cleanly with no startup error, `/v1/models` behavior unchanged (405) |
| T171 | Bump/patch git2 and memmap2 to clear rust:audit RUSTSEC findings | backend-developer | 2026-08-01 | `docs/tasks/task-T171.md` execution notes — memmap2 0.9.10→0.9.11 (RUSTSEC-2026-0186, fixed); git2 fix blocked by Rust 1.87 MSRV requirement, reverted, scoped `cargo audit --ignore RUSTSEC-2026-0183 RUSTSEC-2026-0184` added to `.gitlab-ci.yml` pending T172; bonus fixes for anyhow (RUSTSEC-2026-0190) and wasmtime (RUSTSEC-2026-0222), both discovered live during this task. Final `cargo audit` exit 0. |
| T172 | Bump Rust toolchain to 1.87+ to unblock git2 RUSTSEC fix | devops-engineer | 2026-08-01 | `docs/artifacts/rust-toolchain-1.87-bump-verification-v1.md` — all 3 Rust Dockerfiles + all 3 Rust CI jobs bumped rust:1.86→1.87; git2 0.20.4→0.21.0 (resolves RUSTSEC-2026-0183/0184); `cargo audit --ignore ...` flags from T171 removed, `cargo audit` now exits 0 with zero ignores. Verified: full workspace build+test (all 5 crates, 0 failures), `cargo fmt --check` clean, real `docker build` for all 3 affected images. |
| T165 | Author v0.5.0 changelog and release artifact | technical-writer | 2026-07-27 | Ledger-hygiene retroactive archival (row had been left `pending` despite the work being done): `docs/artifacts/release-v0.5.0.md` (commit `c8c9056`, merged to `develop` via `8031538`); `CHANGELOG.md` `## v0.5.0 - 2026-07-27` section present in current tree |
| T166 | Cut release/v0.5.0 and merge to main | release-manager | 2026-07-27 | Ledger-hygiene retroactive archival: `main` HEAD is the release-merge commit `dd6fbb4` ("Merge branch 'chore/T166-reconcile-main-release-v0.5.0' into 'main'", MR !78) |
| T167 | Tag v0.5.0 and publish GitLab release | release-manager | 2026-07-27 | Ledger-hygiene retroactive archival: git tag `v0.5.0` exists (`git show -s v0.5.0`), points at `dd6fbb4`, tagged 2026-07-27 19:08:28 +0200 |
| T168 | Back-merge main into develop and clean up | release-manager | 2026-07-27 | Ledger-hygiene retroactive archival: back-merge commit `677f9db` "chore: back-merge v0.5.0 release into develop" confirmed present in `develop` history. **Correction (2026-08-01, discovered during T174):** `677f9db` was a flat single-parent commit, not a real merge — `main` was never actually recorded as a git ancestor of `develop` despite this row's original claim. Fixed for real by T177. |
| T173 | Author v0.5.1 changelog, release artifact, and release-ready checkpoint | technical-writer / release-manager | 2026-08-01 | `docs/artifacts/release-v0.5.1.md`; `CHANGELOG.md` `## v0.5.1 - 2026-08-01` section; `docs/checkpoints/checkpoint-018-v0.5.1-release-ready.md` — all on branch `docs/T173-release-v0.5.1-notes`, uncommitted, pending maintainer review/MR (not pushed, no MR opened, per instruction) |
| T177 | Fix develop/main ancestry break (redo v0.5.0 back-merge as a real merge) | release-manager | 2026-08-01 | Real two-parent merge commit `83ca468` on `develop` (MR !88, `merge_commit_sha` set, `squash_commit_sha: null`); `git merge-base --is-ancestor origin/main origin/develop` succeeds; tree verified byte-identical to pre-fix `develop` (no content drift). First attempt (MR !87) was silently squash-merged by GitLab due to project `squash_option: default_on`; corrected by explicitly patching `squash: false` via direct API before merging — see `docs/tasks/task-T177.md` execution notes |
| T174 | Cut release/v0.5.1 and merge to main | release-manager | 2026-08-02 | `main` HEAD is merge commit `8e1a479` ("Merge branch 'release/v0.5.1' into 'main'", MR !90), parents `dd6fbb4` + `d55335a`, real two-parent merge (`squash_commit_sha: null`). First attempt (MR !86, before T177 landed) hit 292 false conflicts against `main`; re-cut from the T177-fixed `develop` confirmed zero conflicts. CI green 11/11 on `release/v0.5.1`. See `docs/tasks/task-T174.md` execution notes |
| T175 | Tag v0.5.1 and publish GitLab release | release-manager | 2026-08-02 | Tag `v0.5.1` on `main` HEAD `8e1a479`; GitLab release published from `docs/artifacts/release-v0.5.1.md`: https://gitlab.com/em-age/emage.code.cwso/-/releases/v0.5.1; RELEASE VERDICT: PASS, reported to user in full |
| T176 | Back-merge main into develop and clean up | release-manager | 2026-08-02 | Back-merge commit `e021262` ("Merge branch 'main' into 'develop'", MR !93), real two-parent merge (`squash_commit_sha: null`); `git merge-base --is-ancestor origin/main origin/develop` succeeds; `develop`'s post-merge pipeline green 11/11; `release/v0.5.1` deleted (origin + local). Closes the T169-T176 v0.5.1 release chain |
| T178 | Add `build:rollout` CI job to `.gitlab-ci.yml` | devops-engineer | 2026-08-03 | `.gitlab-ci.yml` now includes `build:rollout` (extends `.docker-base`) with the same rules as sibling build jobs and script `docker build -t $CI_REGISTRY_IMAGE/rollout:$CI_COMMIT_SHORT_SHA -f deploy/Dockerfile.rollout .`; validated with `rg` and `glab ci lint` |
| T179 | Expand `deploy:registry` to all 4 services + semver tags | devops-engineer | 2026-08-03 | `.gitlab-ci.yml` `deploy:registry` now needs `build:merge-engine` and `build:rollout`, ensures local images for all four services, pushes `:latest` for all on main and `:$CI_COMMIT_TAG` on tag pipelines via loop; validated with `rg`, `glab ci lint`, and local `docker build -f deploy/Dockerfile.rollout .` |
| C001 | README version truth + CI drift guard | devops-engineer | 2026-08-15 | `CHANGELOG.md` backfilled `## v0.6.0` and `## v0.6.1` sections; `README.md` status table now states v0.6.1 as current; `scripts/check-version-drift.sh` (new) compares newest CHANGELOG heading vs README "Current state" and exits 1 on mismatch; `.gitlab-ci.yml` `check:version-drift` job runs it on every pipeline. MR !105 (`agent/devops-engineer/C001`), merged to `develop`. |
| C002 | Reconcile quick-start commands | technical-writer | 2026-08-15 | `README.md` and `docs/user/installation-v3.md` quick-start blocks made byte-identical (`--profile phase2 --profile phase4` sequence), each annotated with an HTML comment noting the blocks must stay identical until C010 removes the profile gates. MR !106 (`agent/technical-writer/C002`), merged to `develop`. |
| C003 | Publish docs/DEBT-REGISTER.md | technical-writer | 2026-08-15 | `docs/DEBT-REGISTER.md` (new, 141 lines) — consolidated register of archived phase scorecards + in-code `POC-DEBT` markers, each row dispositioned (`v1.0-blocker`/`v1.1`/`wontfix`/`fixed`) per roadmap §1.3 (B1–B13). MR !107 (`agent/technical-writer/C003`), merged to `develop`. |
| C004 | Reconcile task ledger with briefs | technical-writer | 2026-08-15 | `docs/tasks/active-tasks.md` rewritten to carry one row per non-`done` `task-T*.md` brief (40 T-rows added, verbatim status); `docs/artifacts/task-ledger-reconciliation-v1.md` (new) — discrepancy report. Flags an unresolved pre-existing issue for orchestrator disposition: ~40 non-`done` briefs (T082–T189 range) also appear as rows in `completed-tasks.md` from earlier "board cleanup" archival passes, i.e. brief status vs completed-ledger status contradict each other — reported, not fixed, per this task's rails (terminal transitions are orchestrator-only). MR !108 (`agent/technical-writer/C004`), merged to `develop`. |
| C005 | Publish docs/SCOPE-v1.0.md | technical-writer | 2026-08-15 | `docs/SCOPE-v1.0.md` (new, 56 lines) — verbatim roadmap §1.5 "what v1.0 means" definition + §2.4 non-goals table (Deferred/Status/Re-entry), with a "How to use this file" section for MR reviews. MR !109 (`agent/technical-writer/C005`), merged to `develop`. |
| T190 | Bump pinned Go CI image to clear govulncheck stdlib advisories | devops-engineer | 2026-08-16 | `.gitlab-ci.yml` `go:lint`/`go:test`/`go:audit` images bumped `golang:1.25.12` → `golang:1.25.13` (all three identical); verified via official `go1.25.13` toolchain (`go vet`, `go test -race`, `govulncheck ./...` → "No vulnerabilities found") plus a live green MR pipeline covering all jobs. Resolves checkpoint-022 blocker BLK-022-01 (pre-existing `go:audit` failure on `develop`'s own tip, unrelated to any Phase 0 change). MR !111 (`agent/devops-engineer/T190`), merged to `develop`; confirmed `develop` tip pipeline green post-merge (pipeline `2763497584`). |
| C010 | Remove phase2/phase4 compose profile gates | devops-engineer | 2026-08-16 | `deploy/docker-compose.yml` — removed stale `profiles: ["phase2"]`/`["phase4"]` gates on `git-shadow`/`merge-engine` (only those two lines + stale comment touched; no other compose key changed); `README.md`/`docs/user/installation-v3.md` quick-starts reconciled (byte-identical, `--profile` flags dropped); `CHANGELOG.md` `## Unreleased` entry. Verified live with Docker: all three services start with no flags, `orchestrator` reaches `(healthy)`, `/healthz` → `ok`. **VERDICT: CONDITIONAL_PASS** (independent Tech Lead review, MR !113) — C010's own diff confirmed correct/complete; tracked condition attached to **C012**, not C010 (see C012 row below and `docs/tasks/task-C012.md`). MR !113 (`agent/devops-engineer/C010`), merged to `develop`. |
| C030 | MCP gap table (impl vs spec) | backend-developer | 2026-08-16 | `docs/artifacts/mcp-gap-analysis-v1.md` (new, 261 lines) — Methods (16 rows incl. lifecycle), Notifications (9 rows), Error codes (2 sub-tables) vs MCP spec `2025-03-26`, each with spec-section + code citations and self-checks confirming exhaustive coverage; 6 Ambiguities recorded (not guessed), including a genuine spec-conformance defect (`notifications/resources/list_changed` advertised but never published). No code touched, no adoption recommendation made (correctly deferred to C031). **VERDICT: PASS** (orchestrator self-review, authorized as proportionate for a docs/analysis-only, no-code-touched change). MR !112 (`agent/backend-developer/C030`), merged to `develop`. |
| C012 | Bootstrap .env.jwt.dev on first run | devops-engineer | 2026-08-16 | `scripts/cwso-bootstrap-secrets.sh` (new) — generates a 64-hex-char secret via `openssl rand -hex 32` (`/dev/urandom` fallback noted), file born at mode 600 via umask (no world/group-readable window), idempotent (second run leaves existing secret untouched), `.gitignore` coverage independently confirmed via `git check-ignore -v`; secret value never printed to stdout/logs at any code path. `CHANGELOG.md` `## Unreleased` entry. **VERDICT: CONDITIONAL_PASS** (independent Tech Lead review, MR !115) — script itself fully verified correct; condition NOT closed by this task — nothing yet calls the script (no `make up` target exists), so the release-gating condition **moves to C016** (see `docs/tasks/task-C016.md` § "Release-gating condition" and the `active-tasks.md` footnote ¹, now attached to C016's row). MR !115 (`agent/devops-engineer/C012`), merged to `develop`. |
| C011 | Add cwso-rollout behind opt-in profile | devops-engineer | 2026-08-16 | `deploy/docker-compose.yml` — added `rollout` service (built from `deploy/Dockerfile.rollout`) gated behind an opt-in `profiles: ["rollout"]` (default `docker compose up` does not start it); mirrors sibling services' hardening posture (`read_only`, `cap_drop: ["ALL"]`, `security_opt: no-new-privileges`, non-root, shared `cwso-runtime` volume) with one documented compose-comment exception (no writable Parquet trajectory-store mount, since that path stays feature-flag-disabled under this profile); env vars checked against `orchestrator/internal/config/` before wiring, nothing invented; reuses the existing `jwt_secret` compose secret, no new credentials. `README.md` one-line mention, `CHANGELOG.md` entry. **VERDICT: PASS, no conditions** (independent Tech Lead review, MR !119) — profile gating, hardening parity, env-var provenance, and file ownership all independently verified. MR !119 (`agent/devops-engineer/C011`), merged to `develop`; first of the sequential `deploy/docker-compose.yml` chain (C011 → C014 → C019), unblocking C014 (dispatched immediately after merge). |
| C017 | scripts/cwso-doctor.sh diagnostics | devops-engineer | 2026-08-16 | `scripts/cwso-doctor.sh` (new) — pre-flight/post-flight diagnostic checking, in the brief's required order, docker/compose availability, port 8080, `/dev/kvm` and vhost-net presence (mirroring `sandbox/router.go`'s `resolveFirecracker()` degraded-mode conclusion without reimplementing it — correctly `[WARN]` not `[FAIL]`), `.env.jwt.dev` existence/gitignore status, sidecar UDS sockets, `/healthz`, and token acceptance (gracefully degrades to `[WARN]` if C013's token script isn't present yet); prints a suggested fix after every `[WARN]`/`[FAIL]`; exits 0 unless a `[FAIL]` was printed; never prints secrets/tokens. `make doctor` target added. **VERDICT: PASS, no conditions** (independent Tech Lead review, MR !117) — check order, exit-code logic, suggested-fix presence, secret non-leakage, graceful degradation, and faithful (non-reimplemented) sandbox-router mirroring all independently verified, including a live no-KVM warn-path test. MR !117 (`agent/devops-engineer/C017`), merged to `develop`. |
| C031 | ADR-013: SDK vs conformance suite | solution-architect | 2026-08-16 | `docs/decisions/ADR-013-mcp-protocol-path.md` (new) — records the human's 2026-08-13 decision (keep hand-rolled MCP kernel, reject SDK adoption for v1.0) with a non-strawman SDK evaluation (determinism named as the disqualifying factor: the official SDK's public docs describe async-per-session dispatch with no documented sequential-mode guarantee, vs. the kernel's flat synchronous `Handle()`), a gap-table-grounded conformance-suite scope for C032 (35 table rows, ~45-55 test cases, 2 flagged required fixes), 5 falsifiable reversal criteria, and an approval-required section. **VERDICT: CONDITIONAL_PASS → resolved** (independent Tech Lead review, MR !118) — one fix required: three citations of `server.go:799-827` corrected to `789-827` (matching the gap table's citation) to avoid clipping the function's opening signature from the cited range; citation accuracy (spot-checked), SDK fairness, reversal-criteria falsifiability, conformance-suite usability, and the ADR-012-missing format justification all independently verified PASS; fix applied and pushed by the orchestrator (Solution Architect has no git/Bash tooling). MR !118 (`agent/solution-architect/C031`), merged to `develop`; unblocks C032 (not yet dispatched). |
| C013 | scripts/cwso-token.sh replaces JWT heredoc | devops-engineer | 2026-08-16 | `scripts/cwso-token.sh` (new) — mints a dev MCP JWT (`--role orchestrator\|worker`, `--ttl`, defaults `orchestrator`/3600s), replacing the inline Python heredoc in `docs/user/installation-v3.md` §3; reads the signing secret from `.env.jwt.dev`, fails with a clear pointer to `scripts/cwso-bootstrap-secrets.sh` (C012) when absent; prints only the token on stdout, all diagnostics to stderr. Claims (`alg` HS256, `iss` `cwso`, `aud` `cwso-mcp`, `role`) verified against the real server validation code and against a running orchestrator container (both roles accepted 200, garbage tokens rejected 401). **VERDICT: PASS, no conditions** (independent Tech Lead review, MR !116) — secret never leaked on any code path including error/fallback branches, stdout/stderr discipline confirmed line-by-line, claim shape cross-checked against actual server code, role/TTL handling correct, file ownership clean. Needed 5 attempts on its `e2e:phase2` CI job before landing (see checkpoint for the retry history) — root-caused as transient runner contention, not a content regression; the 5th isolated attempt succeeded cleanly. MR !116 (`agent/devops-engineer/C013`), merged to `develop`. |
| C014 | Fold enable-all-features into compose defaults | devops-engineer | 2026-08-16 | `deploy/docker-compose.yml` — folded 20 consumed variables onto `orchestrator` (`CWSO_HAL_SOCKET`, `CWSO_HHD_*` x10, `CWSO_SPARSE_*` x3, `CWSO_AST_SPIKE_*` x2, 4 `CWSO_ROLLOUT_*` flags) and 1 (`CWSO_ROLLOUT_HTTP_BIND`) onto `rollout`, each independently traced to a real consumer in `orchestrator/internal/config/config.go` / `services/cwso-rollout/src/config.rs`; values unchanged from `scripts/cwso-enable-all-features.env.example`, only relocated; no secrets moved (those stay in `.env.jwt.dev`). `scripts/cwso-enable-all-features.sh` marked deprecated verbatim per brief (not deleted). `docs/user/installation-v3.md` §10 (not §2 as the brief said — worker caught and correctly fixed the brief's own section-reference error) no longer includes the manual `source` step. **VERDICT: CONDITIONAL_PASS → resolved** (independent Tech Lead review, MR !121) — every moved variable independently re-traced to its consumer (reviewer's own count matched: 20 on orchestrator including all 10 `CWSO_HHD_*`, 1 on rollout), no secrets moved, deprecation header verbatim-correct, §2→§10 self-correction verified legitimate, file ownership clean; one issue found — `CHANGELOG.md` prose undercounted (said 19/x9, should be 20/x10, MR description's own evidence table already had it right) — trivial fix-up commit applied directly by the orchestrator per the reviewer's explicit recommendation, no re-review needed. MR !121 (`agent/devops-engineer/C014`), merged to `develop`; second of the sequential `deploy/docker-compose.yml` chain (C011 → C014 → C019), unblocking **C019** (dispatched immediately after merge). |
| C019 | Sandbox trustworthiness, non-KVM default path | backend-developer | 2026-08-16 | `docs/artifacts/sandbox-trustworthiness-v1.md` (new) — defines 4 testable trustworthiness properties (P1-P4: filesystem confinement, process isolation, resource limits, network policy) for the non-KVM sandbox tier, audits each against `orchestrator/internal/sandbox/*` with live evidence, concludes all 4 MET, states plainly what the tier does NOT guarantee (feeds C063). `deploy/docker-compose.yml` — adds `network_mode: "none"` to `git-shadow`/`merge-engine` (independently re-verified UDS-only, zero networking-crate/std::net usage, no remote-git ops) and `pids_limit` cgroup bounds to all four services, every key with a justifying comment; no existing hardening weakened. A prior same-task session was killed mid-work by an infra session limit; the redo treated its uncommitted artifact/diff as unverified input, independently re-checked every citation, fixed several (wrong line ranges, wrong function attribution, a synthetic verification transcript replaced with a real live `docker compose` run), and reran the sandbox test suite live (33/33, corrected a miscounted "30"). No hard-stop condition triggered. **VERDICT: CONDITIONAL_PASS** (independent security-engineer review, MR !123; required by this task's own brief, not just Tech Lead) — 0 critical, 0 high; every independently-checkable claim reproduced cleanly (all 4 property verdicts, the `network_mode: none` safety claim via the reviewer's own crate-source grep, diff scope, the "`cwso-smoke-test.sh` doesn't exist" claim); artifact found to *under*-claim rather than overclaim. Conditions: **SEC-C019-01** (Medium, structural — C019's P1-P4 evidence covers container-level tiering only, NOT the `pathGuard`/`fs_tools.go` in-process trust boundary that actually matters for C015's read-write mount; tracked forward onto `docs/tasks/task-C015.md`, not closed here, C015 must not cite "P1-P4 MET" as blanket justification) — **SEC-C019-02** (Low, optional, non-blocking — `rollout`'s `pids_limit` rationale comment is less rigorous than git-shadow/merge-engine's; opportunistic backlog, not required) — **SEC-C019-03** (Low, operational — confirm pipeline green before merge; done, pipeline was fully green, `rust:audit`/`go:audit`/`rust:test` included). MR !123 (`agent/backend-developer/C019`), merged to `develop`; closes the sequential `deploy/docker-compose.yml` chain (C011 → C014 → C019). Two pre-existing, unrelated defects incidentally discovered during verification (`.env.jwt.dev` permission mismatch; JWT 401 mismatch in `phase2-integration.py`) logged separately as **T191**/**T192**, not fixed here. |
| T193 | Fix `pathGuard()` symlink-escape gap for new-file writes | backend-developer | 2026-08-16 | `orchestrator/internal/tools/fs_tools.go` — `pathGuard()` now handles the non-existent-leaf case via a new `resolveNearestExistingAncestor()` helper (walks upward via `filepath.Dir` until an ancestor exists, resolves symlinks there, fails closed on any `EvalSymlinks` error that isn't the leaf simply not existing, rather than falling through unchecked as before) and a shared `withinWorkspace()` containment check reused for both the existing-file and new-file paths; the resolved ancestor and the rejoined non-existent tail are each re-verified against the workspace root. Existing-file read/overwrite logic refactored to share `withinWorkspace()`, not rewritten — behavior unchanged. 6 new regression tests including a genuine before/after demonstration (old code: 3 new tests fail, confirmed real out-of-workspace write via a throwaway repro; fixed code: all pass). Full package: 60/60 tests, `go vet`/build clean. Originated from the C015 worker's own investigation of SEC-C019-01 — treated as a hard-stop per its brief's rail rather than shipped around, C015 paused pending this fix. **VERDICT: CONDITIONAL_PASS → no conditions on this task itself** (independent security-engineer review, MR !126; required per this task's own brief) — reviewer independently reproduced the before/after (old code fails the new tests, fixed code passes, matching the worker's claim), verified `resolveNearestExistingAncestor()`'s upward-walk termination and fail-closed behavior on non-ENOENT errors directly in code, confirmed the double containment re-check on the rejoined path, confirmed existing-file behavior is provably equivalent (code-level, not just test-level), and independently reran the full test suite. A new structural finding (TOCTOU: `pathGuard()`'s return value is used by a separate, later filesystem call in each caller, so the check and the use aren't atomic) was surfaced and explicitly assessed by the reviewer as real but not exploitable *today* for a more precise reason than the worker's original "single-writer" framing — no tool in the current MCP surface can create a symlink at runtime at all, so there's no reachable primitive to swap one in during the race window. The reviewer explicitly flagged that precondition as fragile and exactly what C015's read-write mount removes; tracked forward as its own new blocking task, **T194**, not closed here. MR !126 (`agent/backend-developer/T193`), merged to `develop`. |
| T194 | Close TOCTOU gap between `pathGuard()` and its callers | backend-developer | 2026-08-16 | `orchestrator/internal/tools/fs_tools.go` — closes the check-then-use window between `pathGuard()`'s check and each caller's later filesystem operation (`WriteFileSync`, `ReadFileSync`, `ListDir`) via an fd-anchored `openat`/`mkdirat` walk of the directory chain, `O_DIRECTORY\|O_NOFOLLOW` on every hop including the leaf, each hop opened relative to the previous hop's already-resolved fd — no remaining independently-timed name lookup for a race to win against. New regression tests for symlink-at-component and symlink-at-leaf rejection plus a best-effort concurrent race test; all of T193's existing tests unmodified/passing; full module `go test ./... -race` clean; `gofmt`/`vet`/build clean. Requires `//go:build linux` (Go stdlib `openat`/`mkdirat` are Linux-only) — confirmed via cross-compilation to break native non-Linux orchestrator builds; CI and `deploy/Dockerfile.orchestrator` (both Linux-only) are unaffected. **VERDICT: CONDITIONAL_PASS, no blocking conditions on the merge** (independent security-engineer review, MR !127) — reviewer re-derived the fd-anchoring chain line-by-line for all three call sites (confirmed no hop falls back to a fresh path-based lookup), independently reran the full test/vet/build/`-race` suite, confirmed T193's fix and tests genuinely unmodified. On the platform trade-off (the review's central open question, explicitly held for a human decision before merge per the orchestrator's process): reviewer recommended shipping the Linux-only fix as-is given CI/production are unaffected, and tracking the `!linux` portable fallback as a separate, non-blocking fast-follow — human accepted this recommendation. Fast-follow tracked as **T195** (P1, not blocking C015 or this task). MR !127 (`agent/backend-developer/T194`), merged to `develop` — **C015 is now fully unblocked** (both T193 and T194 satisfied). |
| T195 | Portable `!linux` fallback for `fs_tools.go` path guard | backend-developer | 2026-08-19 | `orchestrator/internal/tools/fs_tools_portable.go` (new, `//go:build !linux`) — restores the same exported surface (`ReadFileSync`/`WriteFileSync`/`ListDir`, same struct shape, same tool names) that T194's `//go:build linux`-only `fs_tools.go` had removed from non-Linux builds entirely, fixing a whole-module build break (`orchestrator/internal/server/server.go` references these types unconditionally) and restoring T193/T194's regression-test visibility on non-Linux `go test ./...`. Reuses T193's symlink-resolution logic verbatim; TOCTOU mitigation is the portable "re-verify immediately before use" fallback (not T194's Linux-only fd-anchored atomicity), with an explicit, unburied doc comment stating the narrower guarantee. Race-stress test empirically reproduced a real out-of-workspace write both with and without `-race` (47%/15 runs vs. 7%/15 runs — independently confirmed by security review, not just the worker's claim). **VERDICT: CONDITIONAL_PASS → resolved** (independent security-engineer review, MR !128, dispatched at the same bar as C019/T193/T194 given the empirical reproduction) — fd/re-verify logic re-derived line-by-line, race independently reproduced at a *higher* rate (47%/7%) than the worker's own finding, T193 logic confirmed byte-identical/unregressed, exported-surface parity with `server.go` confirmed; endorsed shipping the honestly-disclosed narrower guarantee (never reachable by CI/production, both Linux-only) over blocking on an unachievable full fix. One blocking condition, **SEV-002** (the disclosure text incorrectly claimed the escape "did not reproduce without `-race`", when it did, just less often) — fixed directly (CHANGELOG text only), re-verified green, merged. Two low, non-blocking backlog items tracked: **SEV-003** (extend race-stress coverage to `ReadFileSync`/`ListDir`, currently only `WriteFileSync` has it) and **SEV-004** (CI/governance guard requiring fresh security review before this file could ever enter an actual build target) — see `docs/DEBT-REGISTER.md` or a future task, not yet formally logged. MR !128 (`agent/backend-developer/T195`), merged to `develop`. |
| T196 | Bump `h2` to clear RUSTSEC-2026-0258 (`rust:audit` CI drift) | devops-engineer | 2026-08-19 | `services/Cargo.lock` — bumped transitive `h2` `0.4.14` → `0.4.16` (lockfile-only, `cargo update -p h2`, no `Cargo.toml` change; `hyper`/`hyper-util` in `services/cwso-rollout` are semver-range constraints so the existing ranges already permitted a patched version) to clear RUSTSEC-2026-0258 ("h2 unbounded empty DATA frames", low-severity DoS). Confirmed content-independent CI drift (same class as T190): reproduced on an unrelated MR whose diff couldn't touch Rust dependencies, and on `develop`'s own tip — an earlier pipeline on the same commit had succeeded, only time (a new advisory) had passed. 146/146 Rust tests pass, 0 vulnerabilities post-fix; the 2 pre-existing "allowed warning" advisories (`fxhash`, `paste`) explicitly left untouched, out of scope. **VERDICT: PASS, no conditions** (Tech Lead review, MR !130) — independently reproduced "0 vulnerabilities" locally with the pinned `cargo-audit` version, confirmed lockfile-only scope and no suppression flags, confirmed the incidental `windows-sys` lockfile reshuffling is an inert no-op (Windows-only conditional deps, Linux-only CI/build target). MR !130 (`agent/devops-engineer/T196`), merged to `develop` — unblocked both T195 (MR !128) and C015 (MR !129), which had independently hit the same drift. |
| C015 | Mount user repo read-write (CWSO_WORKSPACE_HOST) | devops-engineer | 2026-08-19 | `deploy/docker-compose.yml` — parameterized the orchestrator's workspace mount to `${CWSO_WORKSPACE_HOST:-../sample-workspace}:/workspace:rw` (host-side only; in-container `CWSO_WORKSPACE=/workspace` unchanged; `:ro` documented as an explicit escape hatch, not removed) and added a hardened `workspace-check` pre-flight service gating orchestrator startup (`depends_on: condition: service_completed_successfully`) that verifies the resolved workspace path is non-empty — closing a real Docker behavior gap (a missing bind-mount host source is silently satisfied with an empty, root-owned auto-created directory rather than failing; tracked upstream as `docker/compose#13602`). `docs/user/installation-v3.md` §11 documents the read-write default, the shadow-workspace distinction, host permission requirements, and the `:ro` escape hatch, citing both C019's container-level P1-P4 evidence *and* T193/T194's closed in-process `pathGuard`/TOCTOU gaps (not P1-P4 alone) per **SEC-C019-01**. A first attempt at this task was killed mid-work by an infra session limit; the redo treated the salvaged, uncommitted artifact as unverified input — independently re-reproduced the Docker bind-mount claim from scratch, independently re-read `fs_tools.go` to confirm T193/T194's fixes rather than trusting inherited text, live-ran all four brief scenarios (default/custom/nonexistent/`:ro`), and fixed a real section-numbering bug (skipped §11) in the salvaged docs. A second round caught a genuinely new, content-caused CI failure this same task introduced: under this project's Docker-socket-binding CI runners, `workspace-check` correctly detects that the (CI-only, pre-existing, previously-invisible) empty-mount problem is unavoidable there, and fails loudly — fixed via a CI-only `deploy/docker-compose.ci.yml` override (mirroring the existing `.env.jwt.dev` CI-workaround precedent), verified green against the real GitLab pipeline (not just local Docker), confirmed the real `deploy/docker-compose.yml` behavior is unweakened. **VERDICT: PASS, no conditions** (independent security-engineer review, MR !129) — every load-bearing claim re-derived live: Docker's silent-empty-directory behavior reproduced from scratch, all four scenarios re-run against the real stack including the critical negative case, `fs_tools.go` read directly confirming T193/T194 present at all three call sites, SEC-C019-01 substantively verified satisfied (two trust boundaries cited separately with mechanism-level detail, not a blanket P1-P4 citation). One Low, non-blocking finding, **SEC-C015-01** (workspace-check's non-emptiness signal is coarse — a stray file could pass without being a real repo; usability gap, not exploitable, optional follow-up only). MR !129 (`agent/devops-engineer/C015`), merged to `develop`. **Closes the entire C019 → T193 → T194 → C015 security-hardening chain** — this trust boundary (in-process path confinement for a now-read-write, externally-managed repository mount) went through five independent security-engineer reviews (C019, T193, T194, T195, C015) across this single roadmap detour, each catching something real: T193 was a genuine symlink-escape gap; T194 an empirically-adjacent TOCTOU gap; T195 a real, empirically-reproduced (47%) race in the portable fallback; C015 a whole-module CI blind spot the new pre-flight check surfaced. Nothing shipped on a rubber-stamped review. |
| T191 | Fix `.env.jwt.dev` permission mismatch (chmod 600 vs non-root container user) | devops-engineer | 2026-08-19 | `deploy/docker-compose.yml` — first commit added a `jwt-secret-fix` pre-flight helper (hardened: `cap_drop: ALL`, `cap_add: [CHOWN, FOWNER]`, `network_mode: none`, `read_only`, `no-new-privileges`) that looked up the orchestrator image's live `cwso` uid/gid and `chown`ed the host `.env.jwt.dev` in place, mode staying `600`. **Independent Tech Lead review FAILED this first commit** with a live-reproduced regression: the helper's (and, separately, `orchestrator`'s own top-level `secrets: file:` stanza's) bind mount of the host secret path caused Docker to silently auto-vivify an empty, root-owned directory there whenever the file was genuinely absent — corrupting the host filesystem on a fresh clone and breaking `scripts/cwso-bootstrap-secrets.sh`'s recovery path. Second commit redesigned the fix per the review's direction: stopped bind-mounting the host path into any container; `jwt-secret-fix` now mounts the always-existing parent directory, checks for the named file inside it, and (if present) *copies* the secret into a new named Docker volume (`cwso-jwt-secret`) at `/run/secrets/jwt_secret`, owned by the looked-up `cwso` uid/gid, mode `600`; `orchestrator` mounts that named volume read-only at the same path `config.go` already expects (`config.go` itself untouched throughout both commits). Named volumes have no host source path, closing the auto-vivification bug class for both services at once. Also detects and refuses a stray leftover directory rather than corrupting it further. **VERDICT: CONDITIONAL_PASS** (independent Tech Lead re-review, MR !132) — every claim independently live-reproduced on the actual host filesystem (missing-secret scenario: file genuinely stays absent, `config.go`'s original fail-closed error unchanged, clean bootstrap recovery after; happy path: host file mode/owner/**content** unchanged and byte-identical to the staged volume copy — re-review went further than the fix's own claim; stray-directory detection: a planted root-owned directory correctly refused, not corrupted; new `DAC_READ_SEARCH` capability grant confirmed minimal and justified, no `DAC_OVERRIDE`). One process condition (MR description's stale `git diff --stat` claim, corrected post-review) and one CI condition (confirm the correct MR-gating pipeline — not a redundant, infra-flake-affected duplicate — actually finished green) were both satisfied before merge; the MR-gating pipeline itself hit one transient runner hang (`check:version-drift` produced zero trace output for 20+ minutes), resolved cleanly on retry. Incidentally found and independently re-verified during this task (not fixed here, logged separately): `CWSO_IPC_ALLOWED_GIDS` is hardcoded to a gid that doesn't match the orchestrator's actual live gid — confirmed **latent, not an active access-control gap** (the parallel UID allowlist already matches, and the allowlist check is `uid OR gid`), logged as **T197** (P2). MR !132 (`agent/devops-engineer/T191`), merged to `develop` — unblocks **C016** (dispatched immediately after, now correctly listed as a satisfied prerequisite rather than an open risk). |
| C016 | make up one-command target | devops-engineer | 2026-08-20 | `Makefile` — new `up`/`down`/`logs` targets collapsing the prior 7-step manual startup into one command: bootstrap secrets (calls C012's `scripts/cwso-bootstrap-secrets.sh` as a black box) → `docker compose build` → `docker compose up -d` → poll `/healthz` (120s bounded timeout, fails fast and non-zero on timeout or any step failure, no half-started silent states) → mint a token (calls C013's `scripts/cwso-token.sh` as a black box) → print an MCP client config block between clear markers. `README.md`/`docs/user/installation-v3.md` quick-start blocks updated to call `make up`, kept byte-identical to each other (diff-confirmed). **VERDICT: PASS, no conditions** (independent Tech Lead review, MR !135) — every functional claim independently live-reproduced, not trusted from transcript: clean-state `make up` reaches a healthy, token-authenticated stack in ~7.7s with zero manual steps (exercising T191's fix with no regression); printed config block valid JSON, its token verified against a real live `POST /mcp` `tools/list` call (200, real 15-tool list back); a secret-leak grep across the *entire* `make up` transcript found zero occurrences of the raw secret hex or `JWT_SECRET=`; `make down` confirmed clean teardown (no residual containers/networks); failure path (pre-occupied port 8080) fails in 6.4s at step 3, before the health poll even starts, non-zero exit, clear message. Reviewer also explicitly confirmed Tech-Lead-only review routing was correct — zero diff in `scripts/*`/`deploy/*`/`orchestrator/*`/`services/*`, pure orchestration glue over already-independently-reviewed components (C012, C013, T191), no new secret-exposure path introduced. All three of the release-gating condition's closing requirements (see `active-tasks.md` footnote ¹) explicitly verified met. MR !135 (`agent/devops-engineer/C016`), merged to `develop`. **Closes the C010 → C012 → C016 (+ T191) release-gating condition, tracked since 2026-08-16** — the last open item blocking `docs/tasks/task-C062.md` ("Release v1.0.0") from being able to cite a working one-command install path. |
| C020 | ADR-012: filesystem projection decision | solution-architect | 2026-08-20 | `docs/decisions/ADR-012-shadow-workspace-filesystem-projection.md` (new) — **GO: materialise-to-tmpfs** (eager per-workspace materialisation into a tmpfs-backed directory at creation time + `inotify`-driven write-back into the git-shadow blob store, with a periodic hash-based reconciliation pass as the correctness backstop for missed/overflowed `inotify` events). Rejects OverlayFS bind-mount (the original P2-1 plan) and FUSE: both require a real `mount(2)`/`fusermount` call from a trusted CWSO process, a capability withheld everywhere in this project's hardened posture (`cap_drop: ["ALL"]` on every service in `deploy/docker-compose.yml`); the chosen mechanism reuses the tmpfs pattern already proven working under that exact posture (`git-shadow`'s own service block, `deploy/docker-compose.yml:361-364`) and extends the already-reviewed T194 `pathGuard`/`secureResolveDirs`/`secureOpenLeaf` TOCTOU-safe path (`orchestrator/internal/tools/fs_tools.go:65/201/256`) rather than opening new security-sensitive surface. All 3 mechanisms × 4 host-matrix rows (Linux+KVM, Linux no-KVM/degraded, rootless Docker, SELinux-enforcing) scored, 12/12 cells, none blank; materialise-to-tmpfs is the only mechanism scoring "works" (not "needs-elevated-privilege") on all four. Honestly flags a real deviation from the task's literal option name ("materialise-to-tmpfs-**on-open**"): the eager-create-plus-write-back design is a reinterpretation, not a literal on-open lazy trigger (that would require FUSE-style syscall interception, which this option specifically avoids) — surfaced prominently in its own "A naming note, addressed honestly" section and as an explicit approval-sign-off item, not buried. Also honestly hedges cells resting on general Linux/gVisor/SELinux semantics rather than a CWSO-specific test (no artifact in the repo documents a minimum supported kernel version, confirms CWSO has run on rootless Docker or SELinux-enforcing hosts, or confirms gVisor's FUSE-passthrough maturity on this project's pinned version) rather than asserting unverified confidence. C025 (documented IPC-only fallback) explicitly **not** activated — this is a GO, not a NO-GO. **VERDICT: PASS, no conditions** (independent Tech Lead review, MR !142) — every load-bearing citation independently re-verified directly against source (not trusted from the ADR's own prose): the `POC-DEBT (P2-1)` marker at `services/cwso-git-shadow/src/main.rs:11` confirmed verbatim; the `git-shadow` tmpfs config and `cap_drop: ["ALL"]` posture at `deploy/docker-compose.yml:361-364` confirmed real and correctly attributed; `orchestrator/internal/sandbox/router.go`'s `resolve`/`resolveDockerTrusted`/`resolveFirecracker` behavior (100% of no-KVM traffic silently routes to gVisor) confirmed accurate; `pathGuard`/`secureResolveDirs`/`secureOpenLeaf` line citations in `fs_tools.go` confirmed exact; host-matrix completeness (12/12 non-blank cells) confirmed; the `cap_drop`-blocks-mount-but-not-tmpfs core architectural argument (container runtime performs tmpfs mounts at container-creation time, before the container process's own dropped capabilities apply) independently assessed as sound; hedged cells confirmed genuinely hedged (grepped the repo, found no documented minimum kernel version or rootless-Docker/SELinux deployment-target claim anywhere) rather than manufactured false modesty; house format (`docs/decisions/_template.md`) compliance, correct ADR-012 numbering (confirmed ADR-008–011 and ADR-013 already existed, ADR-012 genuinely free), `Status: proposed` (not prematurely self-approved), and the "Approval required" section's four explicit sign-off items all confirmed present and substantive. File ownership confirmed clean (exactly one new file, no code or other docs touched). MR !142 (`agent/solution-architect/C020`), merged to `develop`. Solution Architect has no git/Bash tooling — the orchestrator independently spot-checked the same load-bearing citations before pushing the branch and opening the MR on the agent's behalf (mirroring the C031 precedent). **Human approval required before C021 (implementation) may start** — see the ADR's "Approval required" section for the four explicit items needing sign-off (the mechanism choice itself; the on-open naming reinterpretation; that OverlayFS/FUSE are rejected-for-v1.0 not closed-forever, with the stated Reversal criteria as the correct reopening bar; confirmation C025 stays dormant) — `active-tasks.md`'s C021 row now carries this as an explicit blocking dependency alongside C020 itself. |
| C032 | Execute ADR-013 decision | backend-developer | 2026-08-20 | `orchestrator/internal/server/mcp_conformance_test.go` (new, 16 test functions) + `orchestrator/internal/server/server.go` + `orchestrator/internal/mcp/protocol.go` — executes ADR-013's "keep the hand-rolled kernel, prove it" decision: a conformance suite asserting spec-shaped requests/responses/errors for the 4 gap-table "Implemented" methods (`ping`, `notifications/initialized`, `tools/list`, `tools/call`), the 6 "Partial" methods (`initialize`, `resources/list`, `resources/templates/list`, `resources/read`, `resources/subscribe`, `resources/unsubscribe`), correct spec-shaped `MethodNotFound` (-32601) errors for the 6 genuinely "Missing" methods (`prompts/list`, `prompts/get`, `logging/setLevel`, `completion/complete`, `sampling/createMessage`, `roots/list`), and a sweep confirming 7 notification types are never emitted. Two required fixes surfaced and applied during implementation: (1) `capabilities.resources.listChanged` flipped `true` → `false` in `handleInitialize` because `notifications/resources/list_changed` is never actually published anywhere — the server was advertising a capability it doesn't deliver; (2) a new `mcp.RequestError{Code, Err}` type distinguishes malformed-JSON (-32700, Parse error) from wrong-protocol-version/missing-method (-32600, Invalid Request), previously collapsed into one code. `ErrUnauthorized` (-32001) removed as genuinely dead code (auth is fully handled at the HTTP transport layer, confirmed via a whole-repo grep, not just `internal/mcp/`, for any reachable call site — none found). `POC-DEBT` hand-rolled-subset marker removed from `protocol.go:10`; `docs/DEBT-REGISTER.md` B1 flipped `open`/`v1.0-blocker` → `closed`/`fixed` (C030–C032). Zero tool schema/semantics changes, zero new MCP methods, zero touches to `services/*`/`deploy/*`/`schemas/*` — confirmed via `git diff --stat`, file ownership scoped exactly to `orchestrator/**` and the DEBT-REGISTER B1 row. **VERDICT: CONDITIONAL_PASS → resolved** (independent Tech Lead review, MR !141) — first-round review found the `listChanged:false` fix and `ErrUnauthorized` removal both correct and in-scope, confirmed 100% of the existing test suite green with zero weakened assertions, confirmed `docs/DEBT-REGISTER.md` B1's underlying claim (not just its label) was actually true, but attached two blocking conditions: (1) the MR's own gating pipeline hadn't yet finished green, and (2) a genuine test-coverage gap — `resources/unsubscribe` was only incidentally exercised inside an unrelated notification-sweep test with its own response discarded, no dedicated assertion on its response shape or the unknown-subscription-id error case. Orchestrator independently re-derived condition 2 directly from the diff and the actual `handleResourcesUnsubscribe` handler (confirming the handler logic itself was already correct — a genuine test gap, not an implementation bug) before dispatching a fix. Follow-up commit `177283d` added `TestConformanceResourcesUnsubscribeSpecShapeAndUnknownID` (happy path + unknown-id error case + double-unsubscribe); a second independent Tech Lead re-review verified this non-vacuously via **mutation testing** (temporarily broke the handler's not-found path in a disposable worktree, confirmed the new test actually fails, reverted, confirmed it passes again) and cross-checked the test's expectations against the real handler code directly — condition 2 fully closed. Condition 1 (CI green) was hit twice by transient, content-independent shared-runner flakiness during the review/merge cycle — a `docker compose up` container-name collision between concurrently-scheduled `e2e:smoke`/`e2e:phase4-swarm` jobs (`Conflict. The container name "/cwso-git-shadow" is already in use`, same class as prior C018/T191 incidents) and, separately, the previously-characterized "RPC connection-refused under concurrent load" pattern on a retry of `e2e:phase4-swarm` — both independently confirmed transient by re-running each failed job individually to a clean pass (not assumed), full 14/14 green before merge. Also corrected, non-blocking: the MR description's inaccurate "~21 test functions" claim fixed to the real, grep-confirmed 16. MR !141 (`agent/backend-developer/C032`), merged to `develop` via merge commit `69bb3720`. **Unblocks C033** (client compatibility matrix) and **C034** (contract snapshot test in CI), both listed as depending on C032, not yet dispatched. |
| C018 | E2E smoke test (v1.0 DoD executable) | qa-engineer | 2026-08-20 | `scripts/cwso-smoke-test.sh` (new) — the v1.0 definition-of-done executable: from an already-running stack (`make up`), drives the full real flow over the MCP HTTP transport with a token minted fresh via `scripts/cwso-token.sh` (C013): `health` → `create_shadow_workspace` → `write_shadow_file` → `query_ast` → `commit_shadow` → `merge_concurrent_results` → `teardown` (`drop_shadow_workspace`), one `[PASS]`/`[FAIL]` line per stage, exits non-zero with the failing response body printed on the first failing stage (no fallthrough), `EXIT`-trap teardown (`docker compose down -v --remove-orphans`) runs unconditionally on both the success and failure paths. New `make smoke` target (`up` + the script) and `e2e:smoke` CI job (stage `e2e`, correctly diverges from `.e2e-base` since this script expects an already-running stack rather than starting one itself). Zero mocks, zero hardcoded credentials (both tokens minted fresh via C013). Worker built the script's request payloads against the real, live-verified `orchestrator/internal/tools/shadow_tools.go`/`merge_tools.go` contracts rather than the stale `schemas/create_shadow_workspace.json`/`schemas/query_ast.json` files (a "no mocks" e2e test sending fabricated requests to match wrong docs would defeat the point) and flagged the schema drift as a separate finding instead of silently patching it (out of C018's file ownership) — independently re-verified by both the orchestrator and the reviewer directly against the Go source, logged separately as **T198** (technical-writer, P2, merged same day, MR !138). **VERDICT: CONDITIONAL_PASS → resolved** (independent Tech Lead review; worker's own Tech-Lead-only routing recommendation independently affirmed — pure test harness over already-reviewed, unmodified components (C013's token script, already permission-gated MCP tools), no new secret-handling or path-confinement logic, reuses the established CI ephemeral-JWT-secret pattern from `e2e:phase2`/`e2e:phase4-swarm`) — reviewer live-reproduced both the full-pass run and a deliberate-failure run (git-shadow sidecar stopped mid-run: clean `[FAIL]` on `create_shadow_workspace` with the real dial-refused body, exit 1, no later stages attempted), independently confirmed clean teardown (zero containers/volumes/networks) on both paths via `docker ps -a`/`volume ls`/`network ls`, confirmed the script's actual payload shapes match the real Go `InputSchema()`s exactly (not the stale schema files), confirmed zero mocks/hardcoded credentials via grep, confirmed `glab ci lint` valid and file ownership scoped to exactly `scripts/cwso-smoke-test.sh` (new)/`Makefile`/`.gitlab-ci.yml`/`CHANGELOG.md`. Reviewer additionally found and flagged a smaller drift the worker hadn't: `schemas/merge_concurrent_results.json` is also missing the real, optional `rollout_session_id` field — folded into T198's scope before T198 was dispatched. One blocking condition: the MR's own pipeline (`2775478431`) was failing on `build:rollout` at review time — independently root-caused as an external, transient crates.io supply-chain issue (`arrayref` yanked upstream, breaking `blake3`'s dependency resolution for `cwso-rollout`), confirmed pre-existing-green on `develop`'s own tip ~20 minutes earlier at the same base commit, confirmed **not** a defect in C018's diff (C018 touches no Rust/Cargo files, `build:rollout` isn't even in `e2e:smoke`'s `needs:`). Resolved before merge: confirmed the yank had resolved (a fresh `develop` pipeline came back fully green including `build:rollout`), retried the MR's own gating pipeline (`POST .../pipelines/2775478431/retry`, not a detached branch pipeline — a first attempt via a fresh branch-triggered pipeline was discarded because branch-pipeline `rules:` skip the build/audit/e2e stages entirely and would not have been a valid substitute) — came back fully green (14/14 jobs). Merging then surfaced a second, expected, non-review issue: `docs/tasks/active-tasks.md` had a trivial adjacency conflict against `develop` (T198's row, merged minutes earlier by a separate ledger-logging MR, landed next to C018's own status-line edit) — resolved directly on the MR branch (mechanical, ledger-only, same class as prior small-fix precedents), re-triggered CI, one further transient failure (`e2e:phase2` lost a `docker compose up` container-name race against a concurrently-running `e2e:smoke` job on the same shared docker-socket-binding runner — `Conflict. The container name "/cwso-git-shadow" is already in use`), retried that single job cleanly, `e2e:phase4-swarm` auto-reran alongside it, full 14/14 green, merged. MR !137 (`agent/qa-engineer/C018`), merged to `develop` via merge commit `f3cefd28`. **This is the v1.0 release-gate script** — `docs/tasks/task-C062.md` ("Release v1.0.0") re-runs it before any release can be cut. |
| C034 | Contract snapshot test in CI | qa-engineer | 2026-08-20 | `orchestrator/internal/server/mcp_contract_snapshot_test.go` (new, `TestMCPContractSnapshot`) + `orchestrator/internal/server/testdata/mcp_contract_snapshot_v1.json` (new golden fixture) + `.gitlab-ci.yml` (new `go:mcp-contract-snapshot` job) + `Makefile` (`mcp-contract-snapshot`/`mcp-contract-snapshot-update` targets) — a live-vs-golden contract snapshot test distinct from C032's `mcp_conformance_test.go` (which asserts spec *behavior* per gap-table row): this one captures the **entire** live MCP surface into one JSON fixture and fails loudly on **any** drift, including drift no individual conformance assertion covers. Snapshot contents all sourced live via a single `buildContractSnapshot()` function that feeds both the assertion path and the `-update-snapshot` regeneration path (no separate hand-maintained "expected" data structure): the full 11-tool inventory (name/description/live `InputSchema()`), a fixed probe of all 16 gap-table methods (structural result shape via a value-stripping `shapeOf()` helper, or observed error code), the notification recognized/never-emitted sets, every `mcp` package error-code constant, and representative `tools/call`/`ParseRequest` error-trigger scenarios. Regeneration is deliberate-only (`make mcp-contract-snapshot-update`), never automatic in CI. **Cross-repo alignment** against the sibling `em-age/emage.code` repo's own snapshot test (task T201 there) independently re-fetched via the GitLab API: the 11-tool set matches exactly (byte-identical as a set), plus role-parity, the exact permission-denied message format (`role %q may not invoke tool %q`, code -32002), and the HTTP 403 unrecognized-role behavior (`"forbidden: unrecognised role"`) all independently cross-checked against the real CWSO source at the cited line numbers — no reconciliation needed. **Real finding, correctly handled**: T198 (`schemas/*.json` drift fix) is still `pending`, not merged — `schemas/create_shadow_workspace.json`/`schemas/query_ast.json` still materially diverge from the real Go `InputSchema()`s (confirmed independently, item-for-item matching T198's own drift table) — so the worker deliberately did NOT use `schemas/*.json` as this snapshot's baseline (which the brief's literal instruction would have implied), sourcing tool schemas from the live `tools/list` response instead; a "no mocks"-equivalent judgment call for a contract test, documented in both the test file and the MR rather than silently deviating. Recommends T198 be prioritized. **VERDICT: CONDITIONAL_PASS → resolved** (independent Tech Lead review, MR !148) — file ownership confirmed exactly 4 files, nothing under `orchestrator/internal/*` implementation/`services/*`/`schemas/*`; live-generation (not hand-typed) confirmed by reading the single-source-of-truth design; the drift-detection claim — the single most important thing to verify for a snapshot test — was independently reproduced with a **different probe than the worker's own** (a `list_dir` tool description-string edit in `fs_tools.go` rather than the worker's `ping`-handler field addition), confirming the test genuinely catches drift rather than being scenario-specific or vacuous; build/vet/fmt/full test suite (18/18 packages, 39/39 tests in `internal/server`) independently reproduced; cross-repo alignment and the T198 finding both independently re-verified against source and confirmed accurate. Two conditions, both resolved before merge: (1) the MR's own gating pipeline had one job (`rust:audit`, unrelated to this Go/CI-only diff) still running at review time — confirmed green on a fresh check before merge; (2) the MR description's self-reported test count ("24/24", "23 other tests") was factually wrong (undercounted by ~15) — the reviewer's own independent recount (39 top-level tests, 0 failures) was substituted into the MR description as a post-review correction, no source files changed. MR !148 (`agent/qa-engineer/C034`), merged to `develop` via merge commit `d9f2669d`. |
| C021 | Implement filesystem projection | backend-developer | 2026-08-21 | `services/cwso-git-shadow/src/repo.rs` (fd-anchored materialisation: `ShadowStore::create`/`write_file`/`drop_workspace` wired to a real, tmpfs-backed per-workspace directory `<storage_root>/<workspace-uuid>/`) + `services/cwso-git-shadow/src/main.rs` (module doc rewritten, `POC-DEBT (P2-1)` marker removed) + `docs/DEBT-REGISTER.md` (B2/P2-1 row `open` → `closed`/`fixed`, C021) — implements ADR-012's chosen mechanism (materialise-to-tmpfs), read-side projection + lifecycle wiring only, per its own scope (write-back into the git object store is explicitly deferred to C022, not implemented here — confirmed no inotify/write-back logic present). No new container capability needed: reuses the `git-shadow` service's existing `tmpfs: - /var/lib/cwso/shadow:size=128m,mode=1777` mount under `cap_drop: ["ALL"]` (the exact pattern ADR-012 cited as its load-bearing proof) — `deploy/Dockerfile.git-shadow`/`deploy/docker-compose.yml` confirmed byte-identical/untouched. Base-tree symlink-mode (`120000`) blobs deliberately materialize as inert plain files (content = the link-target string), never a real OS symlink — closes that attack class entirely rather than defending against it. **First-round review: split verdict.** Tech Lead: PASS (scope discipline, no-new-capability claim, symlink-as-plain-file handling, and file ownership all independently verified). Security Engineer: **FAIL — SEC-001 (HIGH)**, a genuine TOCTOU gap in the original `materialize_write()`: it canonicalized and validated containment of the parent directory as a path string, then performed a *separate*, later `open()` that re-resolved the path fresh from the filesystem — `O_NOFOLLOW` only guarded the leaf component, so a symlink swapped into an intermediate directory component between the check and the open would be silently followed, escaping the workspace root. Structurally the same bug class T194 closed in the Go orchestrator, unfixed here in Rust. Per policy, the HIGH finding blocked merge regardless of the Tech Lead PASS — not merged. **Fix (commit `4f0cc28`)**: replaced the canonicalize-then-reopen pattern with an fd-anchored walk mirroring `fs_tools.go`'s `secureResolveDirs`/`secureOpenLeaf` (T193/T194) — `open_root_dir` (the one name-based lookup, justified as the trust anchor since `workspace_dir`'s UUID component is server-generated via `Uuid::new_v4()`, never client-influenced) → `openat_dir_nofollow` for every intermediate component, each anchored to the immediately-preceding hop's already-open fd with `O_DIRECTORY|O_NOFOLLOW` (`mkdirat`-creating as needed, tolerating benign `EEXIST`) → `openat_leaf_nofollow` for the final component, same anchoring → write via the resulting fd itself, never a reconstructed path string. No new dependency (`libc`, already used for the pre-fix `O_NOFOLLOW`, not `cap-std`). Two new tests: a deterministic pre-planted-intermediate-symlink test (honestly documented in-code as *not* distinguishing old from new — the old canonicalize check already caught this static case — included as new regression coverage for a previously-untested intermediate-component case) and a genuine concurrent race-stress test (2000 iterations, a second thread repeatedly swapping a directory for a symlink). The orchestrator independently reproduced the before/after distinction from scratch before dispatching re-review: spliced only the two new test functions onto the untouched pre-fix commit in a throwaway worktree — the race test **genuinely panicked** on the old code (`wrote a file outside the workspace during concurrent symlink swap`) and passed cleanly on the fix. **VERDICT: PASS** (independent Security Engineer re-review, MR !149) — reproduced the same before/after distinction independently from scratch (not trusting the orchestrator's or worker's reproduction), confirmed fd-anchoring is genuinely end-to-end with no path-string fallback at any hop, confirmed the UUID trust-anchor reasoning is sound (server-generated v4 UUID, no client influence), confirmed a necessary NUL-byte `CString` guard the raw `libc` calls need that the old `std::fs` calls got for free (a real addition, not dead code), reproduced the full 22/22 test suite, confirmed zero scope creep into C022 (grepped for inotify/write-back — none present) and zero new dependency (`Cargo.toml`/`Cargo.lock` diff empty). CI green except one non-blocking `rust:lint` fmt nit already configured `allow_failure: true` in this project. MR !149 (`agent/backend-developer/C021`), merged to `develop` via merge commit `94459c76`. **Closes C021 — the first implementation task under ADR-012's GO decision** — unblocks C022 (write-back into the git object store) and C023 (projection lifecycle + crash safety), both listed as depending on C021, not yet dispatched. |
| C022 | Write-back into git ODB | backend-developer | 2026-08-21 | `services/cwso-git-shadow/src/writeback.rs` (new, ~665 lines, `WriteBackEngine`) + `services/cwso-git-shadow/src/repo.rs` (`scan_workspace_tree` reconciliation scan) + `docs/DEBT-REGISTER.md` (B2 row closed for C021+C022; new rows R-3, R-5) — closes the reverse gap from C021: raw external filesystem mutations made *directly* at a shadow workspace's real, projected path (bypassing this service's own IPC entirely — `sed`/`tee`/`rm`/`mv`/an editor) now flow back into the in-memory blob store so `commit_shadow` captures them, for all four mutation types (create/modify via `IN_CLOSE_WRITE`, delete via `IN_DELETE`, rename via independent `IN_MOVED_FROM`+`IN_MOVED_TO`). Implements ADR-012's specified mechanism exactly: `inotify`-driven write-back as the primary path (one dedicated thread, no in-process event buffering) with a periodic hash-based reconciliation pass as the correctness backstop for `inotify`'s documented failure modes (`IN_Q_OVERFLOW`; non-recursive watching of newly-created subdirectories). Write-back is read-only by construction (never writes back to the real filesystem, closing a would-be feedback loop with its own `inotify` events). New dependency: `inotify = { version = "0.11", default-features = false }` (blocking API only, deliberately avoiding the crate's `tokio`-based async "stream" feature this synchronous, thread-per-loop service doesn't use) — confirmed the only new dependency, no unexpected transitive additions. Found and fixed a real deadlock during development (not shipped): an earlier draft shared one mutex between the blocking event-reader thread and the watch-add/remove path, meaning any `create_workspace` call would hang forever waiting on a lock the reader thread held indefinitely inside its blocking read call — fixed by giving the event-reader thread exclusive, unlocked ownership of the raw `Inotify` value and routing watch add/remove through a separate `Mutex<inotify::Watches>` handle. Read-back path safety: every entry's type determined via `symlink_metadata` (never followed, skipped and logged); paths built from kernel-supplied `DirEntry` components only (no string-based traversal, a smaller risk class than the write side by construction, not by assumption). Three judgment calls flagged explicitly by the worker for reviewer disposition, not decided unilaterally, per this project's established discipline: (1) read-side TOCTOU between the `symlink_metadata` check and the subsequent read/recursion, not fd-anchored like the write side (tracked `R-3`); (2) rename decomposed into independent delete+create rather than `inotify`-cookie-correlated pairs; (3) non-UTF-8 filenames silently skipped (`R-4`, pre-existing constraint, surfaced not introduced). **VERDICT: CONDITIONAL_PASS** from both independent reviewers (Tech Lead + Security Engineer, MR !153), both conditions documentation/tracking-only, no code-behavior change required — Tech Lead endorsed the no-cookie-correlation rename design as sound but identified a real, narrow race the worker's own framing hadn't surfaced: the delete-half and create-half are two separate critical sections, and a `commit()` landing in the gap between them could observe the affected file missing under *both* its old and new path (transient full disappearance, self-resolving on the next tick) — condition: document it (done: `writeback.rs` module doc comment, `POC-DEBT R-5`, plus a new non-blocking `v1.1` DEBT-REGISTER row). Security Engineer confirmed R-3 is not exploitable against *today's* mount topology (traced the actual topology: nothing outside `git-shadow` itself currently has any path to the shadow directory) but found the "same actor already has access" justification for accepting it *permanently* rests on an assumption about C024's not-yet-built sandbox-mount wiring — which ADR-012 itself names as an unsolved mount-propagation problem — judged inconsistent with the blocking bar this project already held the equivalent write-side gap to (SEC-001, HIGH, C021); condition: reclassify `R-3` from `v1.1`/`wontfix`-pending to **`v1.0-blocker`**, with a named follow-up task (done: **C035**, new brief, fd-anchored recursive read-back walk generalizing C021's `openat`/`mkdirat` write-side primitives to a tree-walking read via `openat`+`fdopendir`, reviewer confirmed tractable not novel) — if deferred, the "same actor" premise must be explicitly re-confirmed against C024's actual mount-scoping implementation before v1.0 sign-off, not assumed satisfied by C024 merely existing. Both conditions applied directly (doc-comment + DEBT-REGISTER edits, zero logic change, confirmed via a clean rebuild and the full 29/29 test suite still green) rather than requiring a third review round. CI green throughout except the same non-blocking `rust:lint` fmt-nit class as C021 (`allow_failure: true`, confirmed on the job itself; root-caused via trace inspection as a pure whitespace diff, not a flake) and two further instances of this session's now-familiar transient shared-runner container-name collisions, each independently confirmed unrelated to this diff and resolved by retrying the single affected job. MR !153 (`agent/backend-developer/C022`), merged to `develop` via merge commit `70ec89fd`. **This is a good example of the security bar staying consistent across the whole roadmap** — R-3 could easily have been quietly endorsed as an acceptable trade-off on the worker's own reasoning; it wasn't, because the reasoning rested on a premise (future code C024 hasn't built yet) rather than a closed fact, the same standard applied to SEC-001 on the write side. Unblocks **C023** (lifecycle + crash safety, brief already corrected for the real materialise-to-tmpfs mechanism, ready to dispatch) and **C035** (fd-anchored read-back hardening, both P0/`v1.0-blocker`, both sharing broad file ownership with C021/C022's existing code — sequence, do not parallelize, per this roadmap's established practice). |
| C023 | Projection lifecycle + crash safety | backend-developer | 2026-08-21 | `services/cwso-git-shadow/src/repo.rs` (`sweep_stale_workspace_dirs`, called once from `ShadowStore::new`) + `services/cwso-git-shadow/src/main.rs` (module doc: SIGTERM/SIGINT investigation) + `services/cwso-git-shadow/tests/signal_shutdown.rs` (new) — closes the remaining crash-safety gap under ADR-012: a crash (`kill -9`) must not leave stale per-workspace projection directories behind. Startup sweep removes every entry directly under `storage_root` except the persistent `bare.git` ODB directory, unconditionally — sound because `ShadowStore::new` always constructs `workspaces` as an empty map (confirmed directly, both by the worker and independently by the reviewer), so a fresh process never has anything legitimate to reconcile stale directories against; per-entry removal failures are logged and skipped, not fatal to startup. Confirmed `bare.git`'s exclusion is an exact-filename match (can't be accidentally swept, can't accidentally spare a similarly-named stray entry). Deterministic crash test (`fresh_store_sweeps_directory_left_by_simulated_unclean_crash`) genuinely simulates an unclean crash without relying on any `Drop` impl (confirmed none exists) — constructs a real `ShadowStore`, materializes a real workspace directory, discards the instance with no `drop_workspace` call, then constructs a fresh instance against the same `storage_root` and asserts the sweep removes the stale directory. SIGTERM/SIGINT investigation concluded no custom signal handler is needed: default disposition terminates the process immediately, sufficient because neither of `WriteBackEngine`'s background threads buffers anything requiring an orderly flush (per C022's own pre-existing "Durability" doc comment) and `parking_lot` mutexes never poison on panic — confirmed as a real, checkable property of that crate, not an assumption. New `tests/signal_shutdown.rs` integration test spawns the real compiled binary and sends real `SIGTERM`/`SIGINT`, asserting prompt clean exit — run 6 times total across worker + reviewer verification with zero flakiness. Live container-level confirmation was attempted and correctly abandoned as not meaningful: `deploy/Dockerfile.git-shadow`'s entrypoint (`tini -- cwso-git-shadow`, no restart supervisor) means killing the process brings the whole container down with it, making a "kill process, keep container+tmpfs alive" test impossible without modifying deploy files (explicitly out of this task's scope) — the deterministic library-level test is the primary, sufficient evidence, independently confirmed sound by the reviewer reading `Dockerfile.git-shadow` directly. **VERDICT: PASS, no conditions** (independent Tech Lead review, MR !157) — every claim above independently reproduced, not trusted from the worker's report: sweep logic, exact-equality `bare.git` exclusion, per-entry failure isolation, the Drop-free deterministic crash test's actual load-bearing-ness (would fail without the sweep), the `parking_lot` no-poison-on-panic claim, and the full 32 unit + 2 integration test suite (6 total runs of the signal test, zero flakiness). `rust:lint`'s failure confirmed pre-existing and unrelated — reproduces identically at `develop`'s own merge-base commit, `allow_failure: true`, not a regression introduced by this diff. Reviewer also independently confirmed the orchestrator's review-routing judgment (Tech-Lead-only, no Security Engineer): the sweep only enumerates/removes entries under a fixed, non-attacker-controlled `storage_root` via `read_dir`, with no external string ever parsed into a path — a materially different, lower risk profile than C021/C022's new TOCTOU-class write/read logic. File ownership confirmed clean: `services/cwso-git-shadow/**` only, projection mechanism (C021) and write-back sync logic (C022) both confirmed unchanged. MR !157 (`agent/backend-developer/C023`), merged to `develop` via merge commit `b6e671f9`. **Closes C023** — unblocks **C035** (fd-anchored read-back hardening, `v1.0-blocker`, next per the agreed sequencing) and, together with C022, **C024** (E2E proof in CI, still depends on C023 — now satisfied). |
| C035 | fd-anchored recursive read-back walk (harden R-3, v1.0-blocker) | backend-developer | 2026-08-21 | `services/cwso-git-shadow/src/repo.rs` (~692 lines changed) + `services/cwso-git-shadow/src/writeback.rs` (~73 lines changed) + `docs/DEBT-REGISTER.md` (R-3 row) — closes the last open security item in the CG1 filesystem-projection chain. Generalizes C021's linear write-side fd-anchored primitives (`open_root_dir`/`openat_dir_nofollow`/`openat_leaf_nofollow`) to a recursive **read** walk: new `EntryOpen` enum (`Dir`/`File`/`Symlink`/`Vanished`/`Other`) and `open_entry_nofollow` — every directory entry opened via `openat(..., O_NOFOLLOW)` relative to its parent's already-open fd (two independently `O_NOFOLLOW`-guarded attempts: `O_DIRECTORY|O_NOFOLLOW` first, falling back to a generic `O_NOFOLLOW|O_NONBLOCK` open on `ENOTDIR`, classified via `fstat` on the resulting fd, never a path-based `stat`/`symlink_metadata` call) — plus `fdopendir_dup`/`next_dir_entry_name` for directory enumeration via `fdopendir` on a duped fd, never `std::fs::read_dir` on a path string. Both call sites hardened: the reconciliation pass (`scan_workspace_tree`/`scan_dir_into`) and the inotify handler's single-file sync (`writeback.rs`'s new `read_real_file`, replacing the old `symlink_metadata`+`fs::read` pair). Symlink-skip/non-UTF-8-skip policy unchanged — only the enforcement mechanism changed, from a checked-then-trusted path string to fd-anchored kernel-atomic refusal at every hop. No new dependency (`libc`, already used by C021's primitives). **Real, disclosed negative methodology result** (not hidden): the worker's first race-test attempt (a racer mirroring C021's own write-side style — remove/symlink/remove/mkdir in a loop) did NOT reproduce the pre-fix leak even at 50,000 iterations/5s with 8 racer + 4 reader threads — the pre-fix `symlink_metadata`-then-`read_dir`/`fs::read` gap was apparently too narrow for that racer's achievable swap frequency to reliably land in. Switched to an atomic `renameat2(..., RENAME_EXCHANGE)` racer instead — pre-creating both a real directory and a symlink up front and atomically exchanging their names in a loop (no intermediate "resolves to nothing" gap, unlike remove-then-recreate) — which reliably reproduced the leak against the pre-fix code in well under 200ms and passed cleanly against the fix; explicitly flagged this test-methodology substitution for reviewer confirmation rather than deciding unilaterally, since it changes only the racer technique, not the production fix or the security property being proven. 38/38 tests pass. **VERDICT: CONDITIONAL_PASS (Tech Lead) + PASS (Security Engineer)** — Tech Lead's conditions were purely procedural (confirm CI terminal-green; explicitly deferred final authorization to the parallel Security review, consistent with this pairing's established pattern where Security holds the substantive bar on new filesystem-security code). Both reviewers independently reproduced the before/after race distinction from scratch, in their own separate worktrees (not trusting the worker's, the orchestrator's, or each other's reproduction) — both confirmed ~0.04s failure on the pre-fix code and a clean pass on the fix. Both independently assessed the `renameat2` methodology question and converged on the same conclusion: the substitution is sound and the negative result on the weaker racer is itself informative rather than concerning — the underlying gap was structurally real and correctly escalated to blocker status, but practically reachable only via deliberately adversarial code calling an atomic kernel primitive ordinary tools (a real `mv`, an editor's atomic-save-via-rename) never invoke against a name this scan is actively reading. Security Engineer additionally confirmed, independently, the load-bearing property that distinguishes this fix from the accepted-risk posture it replaces: correctness does not depend on any assumption about actor identity — fd-anchoring closes the TOCTOU class structurally, so the fix holds even if C024 later changes who has filesystem access to a shadow workspace's projected directory, unlike the "same actor already has access" reasoning R-3 was originally (provisionally, then rejected) accepted under. CI confirmed genuinely terminal-green (pipeline `2780494629`, `rust:audit`/`go:audit` both green, empty `Cargo.lock` diff confirming no new vulnerable dependency) before merge. MR !160 (`agent/backend-developer/C035`), merged to `develop` via merge commit `f5abd6e0`. **This closes the entire CG1 filesystem-projection security chain**: C020 (ADR-012 decision) → C021 (write-side fd-anchored materialisation, SEC-001 fixed) → C022 (write-back, R-3/R-5 disclosed) → C023 (lifecycle/crash-safety) → C035 (read-side fd-anchored hardening, R-3 closed) — five sequential tasks, five independent review rounds (three security-engineer-gated), zero rubber-stamped verdicts, one HIGH finding (SEC-001) and one reclassified-to-blocker finding (R-3) both fixed for real rather than accepted on a convenient premise. Unblocks **C024** (prove the projection end-to-end in CI — the last task in gate CG2, "real filesystem"). |
