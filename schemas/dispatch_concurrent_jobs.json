{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://cwso.dev/schemas/dispatch_concurrent_jobs.json",
  "title": "dispatch_concurrent_jobs",
  "type": "object",
  "properties": {
    "jobs": {
      "type": "array",
      "minItems": 1,
      "items": {
        "type": "object",
        "properties": {
          "agent_role": { "type": "string" },
          "objective_prompt": { "type": "string", "minLength": 1 },
          "target_workspace_uuid": { "type": "string", "format": "uuid" },
          "sandbox_profile": {
            "type": "string",
            "enum": ["docker-trusted", "gvisor-fast-ephemeral", "firecracker-secure-isolation"],
            "description": "Requested sandbox isolation tier. Server policy enforces routing; callers cannot escalate to docker-trusted."
          }
        },
        "required": ["agent_role", "objective_prompt", "target_workspace_uuid"],
        "additionalProperties": false
      }
    },
    "execution_timeout_seconds": { "type": "integer", "minimum": 1, "default": 300 }
  },
  "required": ["jobs"],
  "additionalProperties": false
}
